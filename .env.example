PORT=3000
JWT_SECRET=请替换成至少32位的随机字符串
# 使用 HTTPS 时设为 true；当前通过 HTTP IP 测试时设为 false
COOKIE_SECURE=false
# 配置后使用常规 PostgreSQL；留空则使用本地 PGlite 数据库
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/digital_card
